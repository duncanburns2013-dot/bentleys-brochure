# Variable NAMES only — never real values. This file is committed.
# Put actual secrets in .env.local, which is gitignored.

# Bridge Data Output server token, scoped to the MLS PIN dataset.
# Only needed if a script has to query MLS directly; the market-share chart
# reads a committed CSV and needs nothing.
BRIDGE_TOKEN=
