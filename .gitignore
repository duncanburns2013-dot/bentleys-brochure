node_modules/
.DS_Store
Thumbs.db
__MACOSX/
*.log

# Working scratch — not deliverables
tmp/

# Build output — regenerated by `npm run build`; Pages builds its own in CI.
dist/

# Secrets — never committed. Names are recorded in .env.example.
.env.local
.env
